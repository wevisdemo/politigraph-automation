[project]
name = "membership_validator"
version = "0.1.0"
dependencies = [
    "poliquery", "hris_scraper", "python-dotenv"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
package = true